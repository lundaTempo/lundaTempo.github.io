{
  "name": "lundatempo",
  "version": "1.0.0",
  "scripts": {
    "build": "sass style.sass style.css && eleventy",
    "watch": "sass --watch style.sass style.css",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "sass": "^1.85.0"
  }
}
